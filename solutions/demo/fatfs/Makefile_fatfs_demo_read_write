TARGET = fpsoc_fatfs_demo_read_write

SDK_ROOT = ../../../

C_SRCDIRS = .

INCDIRS = .

COMMON_FLAGS := -O2 -Wno-int-to-pointer-cast -DMMC_COMPILE_FOR_EMU

# ARM jump from secure to non-secure
# COMMON_FLAGS += -DSUPPORT_NONSECURE


include $(SDK_ROOT)/tools/make/Makefile.base
