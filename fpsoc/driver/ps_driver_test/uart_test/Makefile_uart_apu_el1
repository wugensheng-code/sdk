TARGET = fpsoc_uart_el1

SDK_ROOT = ../../../../

SRCDIRS = .

INCDIRS = ../../ps_driver/uart/inc

CFLAGS := -O2 -DCONFIG_AlUart_Recv_Send_Block_Loop_Back_TEST=1

include $(SDK_ROOT)/tools/make/Makefile.base
