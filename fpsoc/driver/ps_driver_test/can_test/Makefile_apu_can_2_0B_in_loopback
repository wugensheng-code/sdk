TARGET = fpsoc_can_2_0B_in_loopback

NUCLEI_SDK_ROOT = ../../../../

SRCDIRS = .

INCDIRS = .

COMMON_FLAGS := -O2 -DCAN_DEBUG -DCONFIG_CAN_IN_LOOPBACK_TEST=1

# ARM jump from secure to non-secure
# COMMON_FLAGS += -DSUPPORT_NONSECURE


include $(NUCLEI_SDK_ROOT)/Build/Makefile.base
