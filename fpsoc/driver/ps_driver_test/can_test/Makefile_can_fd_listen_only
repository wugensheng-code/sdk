TARGET = fpsoc_can_fd_listen_only

SDK_ROOT = ../../../..

SRCDIRS = .

INCDIRS = .

CFLAGS := -O2 -DCAN_DEBUG -DCONFIG_CAN_LISTEN_ONLY_TEST=1

# ARM jump from secure to non-secure
# CFLAGS += -DSUPPORT_NONSECURE


include $(SDK_ROOT)/tools/make/Makefile.base
