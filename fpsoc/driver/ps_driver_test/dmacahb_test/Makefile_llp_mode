TARGET = fpsoc_dmacahb_llp_mode

SDK_ROOT = ../../../../

SRCDIRS = .

INCDIRS = .

CFLAGS := -O2 -DDMACAHB_DEBUG -DCONFIG_LLP_MODE_TEST=1 -Wno-int-to-pointer-cast

# ARM jump from secure to non-secure
# CFLAGS += -DSUPPORT_NONSECURE


include $(SDK_ROOT)/tools/make/Makefile.base
