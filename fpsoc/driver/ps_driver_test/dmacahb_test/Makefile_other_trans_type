TARGET = fpsoc_dmacahb_other_trans_type

SDK_ROOT = ../../../../

SRCDIRS = .

INCDIRS = .

COMMON_FLAGS := -O2 -DDMACAHB_DEBUG -DCONFIG_OTHER_TRANS_TYPE_TEST=1 -Wno-int-to-pointer-cast

# ARM jump from secure to non-secure
# COMMON_FLAGS += -DSUPPORT_NONSECURE


include $(SDK_ROOT)/tools/make/Makefile.base
