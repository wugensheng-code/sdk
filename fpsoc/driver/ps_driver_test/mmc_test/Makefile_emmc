TARGET = fpsoc_mmc_emmc

SDK_ROOT = ../../../..

SRCDIRS = .

INCDIRS = .

COMMON_FLAGS := -O2 -DMMC_DEBUG -DCONFIG_PERFORMANCE_CALC -DCONFIG_EMMC_TEST=1

# ARM jump from secure to non-secure
# COMMON_FLAGS += -DSUPPORT_NONSECURE


include $(SDK_ROOT)/tools/make/Makefile.base
