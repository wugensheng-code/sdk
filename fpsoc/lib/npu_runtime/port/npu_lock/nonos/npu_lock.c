#include "npu_lock.h"

AL_S8 npu_lock_init() { // nothing need to be done in baremetal
    return 0;
}

AL_S8 npu_lock_destroy() { // nothing need to be done in baremetal
    return 0;
}

AL_S8 npu_lock_lock() { // nothing need to be done in baremetal
    return 0;
}

AL_S8 npu_lock_unlock() { // nothing need to be done in baremetal
    return 0;
}